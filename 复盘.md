# 模型路由 + Agent 路由 课程复盘

> 2026-08-18 会话全量复盘 · 已按 router-lab/ 真实文件与 data/router.jsonl 核实，非虚报

---

## 一、核实结论（真实状态）

- `router-lab/` 现有 **18 个源文件、1751 行 Python**
  - 入口脚本：envcheck / router_v1 / agent_router / gateway / router / compare / report / panel / cost_report / check_secrets
  - 库：lib/llm_client、lib/agent_client、lib/obs、lib/budget、lib/ratelimit
  - 配置：config/providers.yaml、models.yaml、agents.yaml、ratelimit.yaml
  - 文档：ccr-gap.md、keys-inventory.md、docs/ratelimit-design.md（用户自写）
- 观测日志 `data/router.jsonl`：**75 条真实记录**（llm:56 + agent:18 + cache:1），**总成本 0.174 元**，成功率 88%，时间 08:45→13:44
- 结论：全程 70+ 次真实 API 调用、花费约 0.17 元，各里程碑产出真实存在

---

## 二、时间线：做了什么

### 阶段 0 · 地基（里程碑 A）
- 课0.1 环境盘点：三家 key 状态（DeepSeek/Qwen 就绪、Kimi 缺）；确认 claude/codex 都实际跑在 DeepSeek 上（claude 走 api.deepseek.com/anthropic 四槽映射、codex 走 deepseek provider）
- 课0.2 双 CLI 无头打通 + 运行时换模型/基地址（`--model` + env 前缀 + `-c model_provider`）
- 产出 `envcheck.py` 五端可用性矩阵

### 阶段 1 · 模型路由（里程碑 B）
- 课1.1 统一层 `lib/llm_client.py` + `lib/obs.py` + `config/providers.yaml`/`models.yaml`（厂商差异→数据）
- 课1.2 路由动作 `router_v1.py`：manual / round-robin / fallback 降级链 / judge 裁判（JSON 结构化）；抓 2 个 bug（注入传染、inject 参数丢失）
- 课1.3 成本核算 `cost_report.py`（路由后省 74.9%）；Kimi 激活（key 落盘、endpoint=api.moonshot.cn/v1、RPM=3 限速）

### 阶段 2 · Agent 路由（里程碑 E）
- 课2.1 `lib/agent_client.py` 统一封装两 CLI
- 课2.2 多 coding CLI 注册表 `config/agents.yaml`：claude/codex/hermes/qwen/opencode/kimi 全通（gemini 走代理）；修分派 bug、解析器兼容数组/NDJSON
- 课2.3 安全护栏：codex 默认只读沙箱拦写 vs auto 模式（--sandbox workspace-write）放行；抓 codex 两旗标互斥

### 阶段 3 · 网关 + 毕业项目（里程碑 F）
- 课3.1 `gateway.py` OpenAI 兼容 HTTP 网关（/v1/chat/completions 收编模型 API + agent CLI）
- 毕业三合一：router.py（一句话分发）/ compare.py（策略评测，裁判命中率 100%）/ report.py（观测报告）
- 用户自加分布式限流 `lib/ratelimit.py` + `config/ratelimit.yaml`（IP/global/agent 租约 + Redis 降级）

### 阶段 4/5 · 生产化 + 治理
- 课4.1 对标 claude-code-router 差距清单（ccr-gap.md）
- 课4.2 Web 观测面板 panel.py（:8124）
- 课4.3 网关局域网（GATEWAY_HOST 已支持；绑 0.0.0.0 暴露被权限层拦，待用户授权）
- 课5.1 限流实测（IP 滑动窗口 / agent 并发租约 / kimi RPM=3 第 4 次抛 RateLimited）
- 课5.2 预算护栏 lib/budget.py（ROUTER_BUDGET）
- 课5.3 密钥审计 check_secrets.py + keys-inventory.md

### 开源路由五派实操课（A/B/C）
- 阶段A 网关派：LiteLLM SDK 统一层 + Router 组内故障转移实测（proxy 墙内挂，改 SDK）
- 阶段B 智能路由派：语义路由自实现（阈值坑）+ 迷你学习式分类器（RouteLLM 范式 3/3）
- 阶段C CCR 专用派 + 可观测：ccr 路由算法解剖（规则策略引擎）+ Portkey 缓存迷你版进 gateway

---

## 三、关键命令（练习用）

```bash
source ~/.config/aliyun/cc.env          # 加载 DASHSCOPE key（DeepSeek 已在 bashrc）

python envcheck.py                       # 五端可用性矩阵
python router_v1.py --strategy judge --task "..."                # 裁判路由
python router_v1.py --strategy fallback --model deepseek-chat --inject-failure  # 降级演练
python agent_router.py --task "..." --agent qwen                 # 分派到 agent
python router.py "写个快速排序"           # 一句话分发入口
python compare.py                        # 策略评测（多策略对比）
python report.py                         # 观测报告（占比/降级链/最便宜）
python gateway.py &                      # OpenAI 兼容网关 :8123
python panel.py                          # Web 面板 :8124
python check_secrets.py                  # 密钥审计
ROUTER_BUDGET=0.01 python router.py "hi" # 预算护栏演示
```

---

## 四、关键代码地图

- `lib/llm_client.py`：三家统一调用层，`chat(provider, model, route_hint)` → 统一结构 + 观测 + RPM 限速 + 预算检查 + 成本
- `lib/agent_client.py`：`run_agent()` 注册表驱动 + 7 种解析器（JSON 对象/数组事件流/NDJSON/text）+ mode 护栏
- `lib/obs.py`：`record(fields)` 统一落盘，kind=llm|agent|cache|rate_limit
- `router_v1.py`：manual / round-robin / fallback（注入演练）/ judge（JSON 判档，返回 judged_tier）
- `gateway.py`：/v1/chat/completions，model="judge"→API、"agent:xxx"→CLI；GATEWAY_HOST；请求缓存
- `lib/budget.py`：ROUTER_BUDGET 超限拒绝
- `lib/ratelimit.py`：LocalBackend/RedisBackend、Decision、Lease、sliding_window/concurrency、provider_throttle
- 配置：providers.yaml（base_url/key/rpm）、models.yaml（档位/价格/降级链）、agents.yaml（cmd/args/parse/权限）

---

## 五、知识地图

### 核心原理（5 条）
1. 路由=分派；模型路由无状态（喂 prompt 就够），agent 路由要喂"目录+现状+约束+验收"
2. 厂商差异=配置数据（三家都 OpenAI 兼容，只换 base_url/key）
3. 请求名≠实际驱动模型（观测必须记 used_model）
4. CLI 是壳、模型可插拔（claude/codex/qwen/opencode/hermes/kimi/aider/cline 都能指别家；gemini 锁定例外）
5. 护栏默认保守、auto 才显式开

### 算法（8 个）
1. 裁判路由：便宜 LLM → JSON 判难度档 → 选档 → 降级链
2. 降级链：主模型失败 → 按能力等价回退（跨厂商）
3. 滑动窗口限速：60s 窗口计数，超限拒绝 + Retry-After
4. 并发租约：上限 N 并发，超限拒绝/排队，租约自动续期
5. 余弦相似度：向量夹角 → 语义路由
6. 最近中心分类（RouteLLM 范式）：标注集 → 每类中心 → 预测新样本难度档
7. 成本 = tokens/1e6 × 单价（元/百万）
8. 预算护栏 / 请求缓存 / 规则引擎 first-match

### 名词
OpenAI 兼容 / Anthropic 兼容 / OpenAI Responses（三协议）、base_url、API key、provider、RPM、滑动窗口、令牌桶、降级链、虚拟 key、中转站、MCP、NDJSON、stream-json、route_hint、guardrail、sandbox、yolo、auto-approve、embedding、classifier、规则引擎

### 怎么练
- 每课验收点当自测：加新 provider、改 models.yaml 价格跑 compare、调语义路由阈值
- 复刻关键命令（上面第三节）
- 进阶：把 judge 当 ccr 的自定义路由插件、给 rules.yaml 加条件规则

---

## 六、诚实复盘

### 做到了
- 四里程碑 + 六课开源实操全交付
- 8 个真实 bug 靠"预计输出 vs 实际"抓到：
  1. 注入演练传染整条降级链
  2. 修 1 时丢 inject 参数
  3. agent_router 分派 bug（显式 agent 名≠"agent"字符串）
  4. qwen -p 输出是数组事件流
  5. kimi 配置点号模型名要引号
  6. gemini 首跑要 --skip-trust
  7. codex --sandbox 与 --approve-for-me 互斥
  8. 推理模型 30s 超时太短
- 总花费 0.174 元 / 75 次调用

### 失败/暂缓（当时都明说了）
- aider 构建失败（py3.13 源构建）
- gemini 墙内走代理仅小 prompt 稳定
- cline 待 cline auth 配置 provider
- LiteLLM proxy 墙内启动挂（改 SDK 演示）
- semantic-router 库 import 卡（改自实现）
- 网关绑 0.0.0.0 暴露局域网被权限层拦（待用户授权）

### 没做的（最大的遗留差距）
- P2 规则引擎 `config/rules.yaml`（D.1 提了但未执行，文件不存在）
- P3 工具协议桥（codex apply_patch ↔ function tool）
- 语义缓存升级（相似请求缓存，现仅精确匹配）
- 虚拟 key / 内容安全 guardrails

---

## 七、下一步候选

1. 开工 D.1：条件规则引擎 rules.yaml + gateway 接入（补 P2）
2. 网关真暴露局域网（需授权 + 加鉴权/TLS）
3. 语义缓存升级 / 虚拟 key / guardrails
4. 面板接 agent trace
